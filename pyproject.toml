[dependency-groups]
backends = [
 # "maturin",
 "meson-python",
 # "meson-python @ git+https://github.com/mgorny/meson-python@freethreading-limited-api",
 # "scikit-build-core",
#  "setuptools",
  "setuptools @ git+https://github.com/rgommers/setuptools@pyinit-export-fix",
]
build-tools = [
#  "cython @ git+https://github.com/cython/cython@freethreading-limited-api-preview",
#  "cython-cmake",  # for scikit-build-core
#  "nanobind",
]
test = [
  "build",
  "pytest >= 9.0",
  "pytest-xdist",
  { include-group = "backends" },
#  { include-group = "build-tools" },
]

[tool.pytest]
testpaths = ["tests"]
xfail_strict = true

[tool.tox]
requires = ["tox>=4.22"]
env_list = ["3.15", "3.15t"]
skipsdist = true

[tool.tox.env_run_base]
commands = [
  ["pytest", "-n", "auto", "--verbose"],
]
dependency_groups = ["test"]
